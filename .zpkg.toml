[package]
org = "ores-wasm-loaders"
name = "owls-web-loader"
version = "0.1.0"
description = "Extensible browser, worker and server WASM loading"
license = "MIT"
[package.repository]
vcs = "git"
url = "https://github.com/ores-wasm-loaders/owls-web-loader"
[dependencies]
"ores-wasm-loaders/owls-interfaces" = "=0.1.0"
[targets.repository]
dir = "."
[install]
adapter = "none"
[publish]
exclude = [".ores/**", ".zed/**", ".zed-pack/**", "zed_modules/**", "node_modules/**", "coverage/**"]

